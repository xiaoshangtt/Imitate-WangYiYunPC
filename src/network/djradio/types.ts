export interface IDjradioDetailType {
    code: number;
    msg: null;
    data: any;
}

export interface IDjradioProgramType {
    code: number;
    count: number;
    more: boolean;
    programs: any[];
}