import { debounce } from "./debounce";
import { throttle } from "./throttle";
import { toMinutes } from "./toMinutes"
import { formatDate } from "./formDate";
import { regexpChar } from "./regexpChar";
import { toWan } from "./toWan";
import emitter from "./emitter";
import musicP from "./music-player";
import * as SGLocal from "./sgLocal"

export {
    debounce,
    throttle,
    toMinutes,
    formatDate,
    regexpChar,
    toWan,
    emitter,
    musicP,
    SGLocal
}